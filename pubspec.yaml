name: kasir
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🔐 API & Auth
  http: ^1.2.0
  shared_preferences: ^2.2.2

  # 📊 CHARTS & VISUALIZATION (MODERN)
  fl_chart: ^0.66.0  # Lebih modern dan compatible
  intl: ^0.18.0      # Tetap pakai versi terbaru

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true